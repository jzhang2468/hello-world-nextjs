# Hello World

A simple Next.js app created with create-next-app, TypeScript and the App Router.

## Development

Run `npm install` and `npm run dev`, then open http://localhost:3000.

## Validation

Run `npm run lint` and `npm run build`.

## Deployment

Import this GitHub repository into Vercel using the detected Next.js defaults. No environment variables are required.
